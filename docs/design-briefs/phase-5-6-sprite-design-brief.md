# Phase 5/6 Sprite Design Brief

Status: research-only, parked.

This file preserves the handoff named by `docs/README.md` for the deferred Amendment A3 visual polish pass: five pixel-art role sprites plus AuditBeadString, HashChainBadge, and FindingChip chrome.

Current launch scope remains the shipped `apps/web/` dashboard with SSE audit tail, role-state sprite containers, `/debug`, and `/codex`. Do not treat this brief as required for `v-submit` readiness unless a future design/prototyping pass explicitly reactivates it.
